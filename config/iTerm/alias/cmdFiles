******************
Comandos Básicos Terminal
******************

# Copiar Directorios
cp -r directorio/ ruta_de_destino/

# Copiar archivos
cp directorio/ ruta_de_destino/

# ELiminar Directorios
rm -rf /Carpeta

# ELiminar Archivos
rm -r archivo1 archivo2
rm -r /ruta_archivo

# Mover archivo o carpetas
mv archivo /Ruta 
mv * /Ruta

# Renombrar
mv archivo /Ruta/nuevonombre
mv archivo.txt nuevonombre.txt

------------------------------------------------

# zsh
history-clear
cp-github		//Copia .zshrc en github
------------------------------------------------

# fish

# Elimina todos los elementos del historial
history clear

# Muestra una lista de todos los comandos anteriores que contienen la cadena "foo".
history search --contains "foo"

# Elimina de forma interactiva los comandos que comienzan con "foo" del historial.
history delete --prefix "foo"

# Puede seleccionar más de una entrada ingresando sus ID separados por un espacio.
history delete --prefix "20 25 62"
